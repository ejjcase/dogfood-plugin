plugins {
    id 'java-gradle-plugin'
    id 'maven-publish'
}

group = property('group.org')

gradlePlugin {
    plugins {
        greeting {
            id = 'dogfood.plugin.greeting'
            implementationClass = 'dogfood.plugin.DogfoodPluginPlugin'
        }
    }
}

